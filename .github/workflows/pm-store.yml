name: Deploy

on:
  push:
    branches:
      - main

env:
  CI: false

jobs:
  build:
  
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: 16

    # - name: Install dependencies
    #   run: npm install --force

    # - name: Creating build
    #   run: npm run build

    - name: Creating test file
      run : echo test-git >> test.txt

    - name: Doing the magic now
      run: sshpass -p "?@123PM?@1983" scp -o StrictHostKeyChecking=no test.txt root@170.187.253.75:/home
      
    - name: LIVE!!!
      run: echo "Site is live🚀🚀🚀"
